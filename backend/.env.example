# Copy to .env for local dev. In AWS these come from the EC2 launch template /
# SSM parameters, never committed.
PORT=4000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=wedding
DB_PASSWORD=wedding
DB_NAME=wedding_register
